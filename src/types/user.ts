// import {user} from '../models/db/schema'; the user table ormType, will be accessible after followed ## Generate drizzle model types from the readme.md
// import { z } from "zod";

// export const userSelectDb = createSelectSchema(user);
// export type UserSelectDb = z.infer<typeof userSelectDb>;
